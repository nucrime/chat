server:
  compression:
    enabled: true
    mime-types: image/jpeg,text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    min-response-size: 1024
  http2:
    enabled: true
  port: 5000
spring:
  servlet:
    multipart:
      file-size-threshold: 2KB
      max-file-size: 5MB
      max-request-size: 7MB
  data:
    mongodb:
      # %40 in the url stays for @
      uri: mongodb+srv://user:Val!dP%40ssw0rd@cluster0.0gupx.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
